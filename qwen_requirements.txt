# Qwen VLM Fine-tuning Requirements for Safety Assessment
# Core dependencies for Qwen-VL model fine-tuning

# Qwen models and transformers
transformers>=4.37.0
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Qwen specific packages
qwen-agent>=0.0.12
modelscope>=1.9.0

# Data processing and training
datasets>=2.14.0
accelerate>=0.20.0
peft>=0.6.0  # For LoRA fine-tuning
bitsandbytes>=0.41.0  # For quantization
deepspeed>=0.10.0  # For distributed training

# Image processing
Pillow>=9.0.0
opencv-python>=4.5.0
matplotlib>=3.5.0

# Training utilities
wandb>=0.15.0  # For experiment tracking
tensorboard>=2.10.0
tqdm>=4.64.0

# Evaluation metrics
scikit-learn>=1.1.0
numpy>=1.21.0
pandas>=1.4.0

# JSON and file handling
jsonlines>=3.0.0

# Optional: For better performance
flash-attn>=2.3.0  # Install separately if CUDA available
